<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('cookie_definitions', function (Blueprint $table): void {
            $table->id();
            $table->foreignId('cookie_category_id')->constrained()->cascadeOnDelete();
            $table->string('name');
            $table->string('provider');
            $table->string('duration');
            $table->text('purpose');
            $table->boolean('is_first_party')->default(true);
            $table->unsignedInteger('sort_order')->default(0)->index();
            $table->timestamps();

            $table->unique(['cookie_category_id', 'name']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('cookie_definitions');
    }
};
